name: Update OpenLogi Cask

on:
  repository_dispatch:
    types: [update-openlogi]
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g. 0.0.2)"
        required: true

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Determine version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            VERSION="${{ github.event.client_payload.version }}"
          else
            VERSION="${{ github.event.inputs.version }}"
          fi
          VERSION="${VERSION#v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Download DMGs and compute checksums
        id: checksum
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          base="https://github.com/AprilNEA/OpenLogi/releases/download/v${VERSION}"
          for arch in arm64 x86_64; do
            url="${base}/OpenLogi-v${VERSION}-macos-${arch}.dmg"
            code=$(curl -sL -o "/tmp/openlogi-${arch}.dmg" -w '%{http_code}' "$url")
            if [ "$code" != "200" ]; then
              echo "error: DMG not found at $url (HTTP $code)" >&2
              exit 1
            fi
            sha=$(sha256sum "/tmp/openlogi-${arch}.dmg" | awk '{print $1}')
            echo "sha256_${arch}=${sha}" >> "$GITHUB_OUTPUT"
            rm -f "/tmp/openlogi-${arch}.dmg"
          done

      - name: Generate cask
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          mkdir -p Casks

          cat > Casks/openlogi.rb <<EOF
          cask "openlogi" do
            arch arm: "arm64", intel: "x86_64"

            version "${VERSION}"
            sha256 arm:   "${{ steps.checksum.outputs.sha256_arm64 }}",
                   intel: "${{ steps.checksum.outputs.sha256_x86_64 }}"

            url "https://github.com/AprilNEA/OpenLogi/releases/download/v#{version}/OpenLogi-v#{version}-macos-#{arch}.dmg"
            name "OpenLogi"
            desc "Lightweight, local-first companion for Logitech HID++ peripherals"
            homepage "https://github.com/AprilNEA/OpenLogi"

            livecheck do
              url :url
              strategy :github_latest
            end

            auto_updates true
            depends_on macos: :ventura

            app "OpenLogi.app"

            zap trash: [
              "~/.config/openlogi",
              "~/.local/share/openlogi",
              "~/Library/Caches/org.openlogi.openlogi",
              "~/Library/Preferences/org.openlogi.openlogi.plist",
              "~/Library/Saved Application State/org.openlogi.openlogi.savedState",
            ]
          end
          EOF

      - name: Commit and push
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/openlogi.rb
          git diff --cached --quiet && echo "No changes" && exit 0
          git commit -m "Update openlogi to ${VERSION}"
          git push
